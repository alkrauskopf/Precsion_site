<div class="row enroll_header">
  <div class=" col-md-2"></div>
  <div class="col-md-9">
    <h2><%=@event.name %> Enrollment Edit</h2>
  </div>
</div>
<%= form_for(:reservation, :url=> prep_enroll_update_path(:id=> @reservation.id), html: { class: "form-horizontal", role: "form" }) do |f| %>
  <%= render partial: 'form', locals: {:f=> f, :event => @event} %>
<% end %>

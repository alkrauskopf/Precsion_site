<h1 id="title_row">Edit Offering</h1>
<%= render 'form' %>

<div class="row col-md-offset-2">
  <%= button_to 'Delete Offering', admin_offering_path, class: 'btn btn-danger', method: :delete,
                data: {confirm: 'Are you sure? This action cannot be undone'} %>
</div>

<%= render partial: 'pov_associations', locals: { offering: @offering, povs: @offering.user_povs, avail_povs: @offering.available_povs}%>


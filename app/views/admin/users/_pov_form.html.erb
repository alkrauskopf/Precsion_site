
<%= render partial: 'pov_list', locals: {user: user}%>

<%= form_for([:admin,new_pov], html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if new_pov.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(new_pov.errors.count, "error") %> prohibited this new_pov from being saved:</h4>

      <ul>
      <% new_pov.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.hidden_field :user_id, value: user.id %>
    <div class="form-group">
      <%= f.label :label, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :label, class: "form-control" %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :name, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :name, class: "form-control" %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :url, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :url, class: "form-control" %>
      </div>
    </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
